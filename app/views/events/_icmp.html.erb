
<div class='section-title plus'>ICMP Header Information</div>

<div class='proto plus'>

  <table class='ip-header' border='0' cellspacing='0' cellpadding='0'>
    <tr>
      <th class='first'><span title="Type (8 bits) - ICMP type" class='add_tipsy'>Type</span></th>
      <th><span title="Code (8 bits) - Subtype to the given type" class='add_tipsy'>Code</span></th>
      <th><span title="Checksum - Error checking data. Calculated from the ICMP header+data, with value 0 for this field. The checksum algorithm is specified in RFC 1071" class='add_tipsy'>Csum</span></th>
      <th><span title="Identifier (16 bits) - Value of the identifier used in some of the ICMP messages" class='add_tipsy'>ID</span></th>
      <th class='last'><span title="Sequence (16 bits) - Value of the sequence number field used in some ICMP messages." class='add_tipsy'>SEQ</span></th>
    </tr>
    <tbody>
      <tr>
        <td class='first'><%= @event.icmp.icmp_type %></td>
        <td><%= @event.icmp.icmp_code %></td>
        <td><%= @event.icmp.icmp_csum %></td>
        <td><%= @event.icmp.icmp_id %></td>
        <td class='last'><%= @event.icmp.icmp_seq %></td>
      </tr>
    </tbody>
  </table>

</div>