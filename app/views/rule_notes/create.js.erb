$('div#notes_<%= @rule.id %>').prepend("<%= escape_javascript(render :partial => 'rule_notes/note', :locals => {:note => @note} ) %>");

update_note_count('<%= @rule.html_id %>', {notes_count_in_words: "This event has <%= pluralize(@rule.notes.size, 'note') %>", notes_count: <%= @rule.notes.size %>});

$('div#new_note_box').remove();
Snorby.helpers.remove_click_events(false);
$('div#notes_<%= @rule.id %>').find('textarea#body').val('');
$('strong.no-notes').remove();
$('button').removeClass('loading');
flash();