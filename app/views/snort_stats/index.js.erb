$('div#stats').replaceWith("<%= escape_javascript(render(:partial => 'snort_stats', :locals => { :stats => @stats })) %>");

//$('div.content').fadeTo(500, 1);
//$.scrollTo('#header', 500);

Snorby.helpers.remove_click_events(false);
Snorby.helpers.recheck_selected_events();
flash();